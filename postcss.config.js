module.exports = {
    plugins: {
      "tailwindcss/nesting": {}, // Enables support for nesting in Tailwind CSS
      tailwindcss: {}, // Loads Tailwind CSS
      autoprefixer: {}, // Adds vendor prefixes to CSS rules for better browser compatibility
    },
  };
  